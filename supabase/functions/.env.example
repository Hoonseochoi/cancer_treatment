# 슈린슈 AI Edge Function Secrets
#
#   1) 이 파일을 .env 로 복사하고 값을 채운다
#   2) supabase secrets set --env-file supabase/functions/.env --project-ref omgwvnibssizmhovporl
#
# .env 는 커밋하지 않는다(.gitignore 에 등록됨).
# 시스템 프롬프트는 함수와 함께 배포되므로 여기 넣지 않아도 된다.

OPENROUTER_API_KEY=sk-or-v1-여기에_발급받은_키
CLAUSE_ACCESS_CODE=1125

# 선택 — 기본값 deepseek/deepseek-chat
# CLAUSE_MODEL=deepseek/deepseek-chat
