# 패치노트 (CHANGELOG)

프로젝트의 주요 변경 사항 및 업데이트 내역입니다.

## [2026-09-01] PDF 표지 · 머리말/꼬리말
- **기능 추가**: 전체 PDF 저장에 표지 페이지 추가(화이트 바탕 + 네이비 프레임, 옅은 글로우). "OOO님을 위한 / 슈린슈 스마트 가입제안서"와 가입된 분석 항목 칩을 배치. 고객명은 `window.__customerName`으로 노출하며, 추출 실패 시 '고객'.
- **기능 추가**: 본문 각 장에 머리말(화면 이름)·꼬리말(원본 파일 제목, 쪽번호)과 표지와 동일한 네이비 프레임. 표지·프레임 모두 HTML→html2canvas 경로로 생성 — jsPDF 기본 폰트에 한글이 없어 벡터로는 파일 제목을 렌더할 수 없다.
- **기능 추가**: 표지 중하단에 상품설명서 요약 고지, 꼬리말에 "보험사의 공식자료가 아니며 … 참고자료" 문구 배치(전문가명·날짜 표기는 제거).
- **정리**: `forPdf` 플래그 도입. PDF 본문에서 파일 정보 카드(`#file-info`)와 화면 제목 줄을 제외(이미지 단독 저장 경로는 불변).
- **성능**: PDF 삽입 직전 300dpi로 다운스케일. 기존 466dpi로 들어가던 캡처를 조정해 4장 3.1MB → 1.63MB.
- **버그 수정**: html2canvas가 텍스트 베이스라인을 줄상자 아래에 붙여 그려 `.sg-chip`(질수·1종·2종)과 표지 칩의 글자가 아래로 밀리던 문제. 잉크의 절대 위치는 line-height에만 좌우되고 padding-bottom과 무관함을 실측으로 확인하고, 균형식 `pb = pt + 1.35em`을 적용(편차 3.67px → 0.14px).

## [2026-08-31] 1~8종 수술 검색 · 뇌·심장 카드 개편
- **기능 추가**: `1~8종 수술 검색` 모달. 질병명·수술명·수가코드로 검색하면 관련 수술과 ADRG/수가코드, 1~8종 등급, 제안서 업로드 상태에서는 가입금액 기준 계산액까지 표시. 근거는 약관 2-109 제3조②(별표16 수술·시술 코드 = KDRG ADRG).
- **기능 추가**: 검색 결과가 없을 때 bigram Dice 유사도로 근접 항목을 제시하는 fuzzy 폴백. 질병 TOP 30 / 수술 TOP 30 회귀 테스트로 검증하고 동의어 보강.
- **기능 추가**: 모달 하단 고정 면책 안내(참고용, 미용·피임·검사 목적 수술 지급 제외).
- **UI 개편**: 뇌·심장 동심원 → 카드 그리드. 치료행위(수술·혈전용해·혈전제거) + 중환자실 치료비를 한 줄(`2fr 2fr 2fr 1fr`)에 배치하고 금빛 테두리로 강조. 질환별 진단비는 뇌 계열(파랑) / 심장 계열(분홍) 상자로 분리.
- **기능 추가**: 카드 안에 금액을 구성하는 세부 담보를 전개. 통합치료비 행에는 연 한도와 보장주기(수술=매회 / 혈전용해·혈전제거=연 1회) 병기.
- **기능 추가**: 질환 전용 치료·수술비(예: `뇌혈관질환 수술비`)를 해당 진단비 카드에 별도 표기.
- **정리**: 카드와 중복되던 진단비 막대그래프·수술비 구성 표를 제거하고 「보장금액 합계」로 통합. 동심원 SVG 관련 JS/CSS 삭제.
- **문서**: 튜토리얼(about.html) 뇌·심장 섹션 재작성 및 스크린샷 재캡처, `1~8종 수술 검색` 섹션 신설.

## [2026-08-26] 뇌·심장(순환계) 한눈에 보기
- **기능 추가**: 결과 화면에 `뇌·심장` 토글(3번째 탭) 도입. 삼성화재 제안서에서 순환계 담보(`kind:'circulatory'`)가 감지될 때만 노출.
- **기능 추가**: 뇌·심장 동심원. 뇌출혈 ⊂ 뇌졸중 ⊂ 뇌혈관질환, 급성심근경색 ⊂ 허혈성심장질환 포함관계를 원 크기로 표현하고 가입금액을 원 안에 표기. 부정맥·특정3대심장질환은 별개 집합이므로 작은 하트로 분리.
- **기능 추가**: 뇌·심장 그림 하단에 치료행위 카드(수술·혈전용해·혈전제거) 배치. 하단에 최대 보장금액과 산출 근거 표시.
- **기능 추가**: 진단비 막대그래프 분리. 미가입 담보는 점선으로 표시해 보장 공백 노출.
- **기능 추가**: 특정치료비(수술·혈전용해·혈전제거 시 가입금액 지급)와 통합치료비(검사~재활 전 과정 보장)를 구분. 통합치료비 가입 시에만 `검사 ▸ 약물 ▸ 치료 ▸ 수술 ▸ 재활` 치료 경로 차트 노출(14개 항목, 약관 지급표 기준 표준형/실속형 분기).
- **기능 추가**: 특정순환계질환Ⅱ 분류표 41개 항목 물음표 안내. Ⅱ·Ⅲ 보장 범위 동일함을 명시.
- **버그 수정**: 같은 질환에 복수 담보가 붙는 경우(`뇌혈관질환 진단비` + `뇌혈관질환(90일면책) 진단비`) 첫 건만 집계하던 문제. 별개 특약이며 각각 "최초 1회한"이므로 합산(실측 1,000만 → 2,000만).
- **버그 수정**: 담보명에 포함된 금액 표기(`통합치료비(표준형, 연간1억원한도)`)를 가입금액으로 오인해 담보명이 잘리던 문제. 한도/이내/까지 접미 표기는 건너뛰고, 가입금액이 다음 줄로 분리된 레이아웃도 처리.
- **버그 수정**: 수술·혈전용해·혈전제거를 무조건 합산해 과대 표기하던 문제. 약관 지급표가 「수술(혈전제거술 제외)」로 정하고 있어 대표 금액은 최고 단일 치료행위 기준, 3행위 동시는 최대 보장금액으로 분리.
- **로직 수정**: 통합치료비 연 최대 한도(표준형 1억 / 실속형 5천만) clamp 적용 및 한도 적용 여부 표시.
- **문구 수정**: 감액 안내에 180일 미만 10% 구간 추가(기존 "1년 이내 50%"만 안내).

## [2026-08-25] 수술비 한눈에 보기
- **기능 추가**: 결과 화면에 `암 보장 ↔ 수술비` 토글 도입. 삼성화재 제안서에서 수술비 담보가 감지될 때만 노출되며, 감지 건수를 뱃지로 표시.
- **기능 추가**: 다빈도 수술 30종(술기 67개) 분석 뷰. 수술별로 질병수술비·1~5종·1~8종·N대질병 수술비 중 검토 가능한 담보를 분해하고, 약관 수술코드·분류표 항 번호를 근거로 함께 표시.
- **기능 추가**: 술기에 따라 종이 갈리는 경우(예: 결장절제술 림프절절제 동반 7종 / 미동반 5종) 금액을 범위로 표기.
- **로직 수정**: 암 수술비 카드에 질병수술비 및 1~5종·1~8종 수술비 가산. 계층 전파를 통해 다빈치로봇수술비 카드에도 동일 반영. 관혈적 근치수술 5종 / 내시경·경피적 3종 분기로 `totalMin~totalMax` 범위 산출.
- **버그 수정**: 삼성 추출기 화이트리스트가 "암" 키워드 기반이라 질병 1~5종 수술비, N대질병 수술비, 통합 양성신생물 수술비 등이 전부 누락되던 문제(실측 40건). `kind:'surgery'` 태그로 분리 추출.
- **버그 수정**: 질병수술비 특별약관 제6조② KCD 면책 미반영으로 인한 과다 계산(치핵 70만→30만, 제왕절개 60만→30만). 1~5종·1~8종은 별도 특약이라 면책 미적용.
- **버그 수정**: `renderSurgeryPanel`의 `addEventListener` 중첩으로 재분석 시 상세 토글이 동작하지 않던 문제.
- **UI 개선**: 수술 목록 2열 그리드 전환(패널 높이 4,881px → 1,708px). 펼친 행은 전체 너비 사용, 면책 문구 2단 흘림.
- **UI 개선**: 지급액이 동일한 술기를 하나로 병합(`복강경/개복 담낭절제술`). 낱말 경계·괄호 균형을 검사해 잘못 잘린 라벨 방지.
- **UI 개선**: PATCHNOTE 진입 버튼에 NEW 뱃지 추가.
- **로직 수정**: N대질병 수술비 그룹 간 중복 미지급 반영. 한 질병이 여러 그룹에 해당해도 가입금액 최고 1개만 지급하고 나머지는 미지급으로 표시.
- **기능 추가**: 수술비 뷰 이미지 저장 지원. 캡처 시 상세는 전부 접고 토글 버튼은 제외해 목록 형태로 담는다.
- **UI 개선**: 수술비 패널 색상을 CSS 변수로 토큰화. `color-mix` 미사용(html2canvas 1.4.1 호환).
- **UI 개선**: 수술비 안내사항을 접이식으로 변경. 기본값은 상단 리드 문구("표시 금액은 가입금액 기준 검토 가능한 최대 범위이며...")만 노출하고 나머지 10개 항목은 접어두며, 클릭 시 펼쳐진다. 이미지 저장 시에는 현재 펼침 상태와 무관하게 항상 접힌 상태로 캡처.
- **UI 변경**: 다크모드 제거, 항상 라이트로 고정. `style.css`·`index.html`·`about.html`·`patchnote.html`·`insight.html`의 `@media (prefers-color-scheme: dark)` 블록 6개를 모두 삭제하고, `:root { color-scheme: light }`와 각 페이지 `<meta name="color-scheme" content="light">`로 스크롤바·폼 컨트롤까지 라이트 고정. 다크모드에서 인라인 스타일이 오버라이드를 눌러 건너뛰던 `config.js`의 `isDarkMode` 분기도 제거.

## [2026-02-20]
- **기능 추가**: 매니저 환영 플로팅 뱃지 시스템 도입.
- **UI 개선**: 레벨업 알림 위치 및 애니메이션 수정 (뱃지 아래 정렬 및 가독성 확보).
- **데이터 연동**: `manager.txt` 파싱을 통한 실시간 매니저 데이터 로직 최적화.
- **버그 수정**: 알림 메시지가 상단에서 잘리던 현상 해결.

## [2026-02-19]
- **성능 개선**: PDF 텍스트 추출 로직 및 예외 처리 강화.
- **UI 개선**: 대시보드 폰트 사이즈 및 컬러 팔레트 조정 (Premium Look).

## [2026-02-12]
- **로직 수정**: 암 통합치료비(비급여) 1억 보장 관련 추출 로직 정교화.
- **UI 개선**: 로고 사이즈 확대(70px/90px) 및 최신 `ci.png` 동기화.

## [2026-02-11]
- **초기 릴리즈**: 프로젝트 초기 구성 및 깃허브 저장소 연결.
- **기본 기능**: PDF 업로드 및 암 보장 내역 기본 시각화 구현.
- **백엔드 설정**: Supabase 연동 및 기본 테이블 스키마 구성.
