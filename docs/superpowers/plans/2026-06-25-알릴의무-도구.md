# 알릴의무 작성 보조 도구 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** README와 디자인 설계(`docs/superpowers/specs/2026-06-25-알릴의무-도구-design.md`)를 바탕으로, 병력 입력 → 병력표 편집 → 표준알릴의무 Q1~Q5 분류결과를 보여주는 독립 정적 웹페이지를 만든다.

**Architecture:** 빌드 도구 없는 순수 HTML/CSS/JS 정적 페이지. `data.js`(상수/매핑)와 `app.js`(파서+룰엔진+렌더링)를 분리해 로직 변경 없이 데이터만 갱신 가능하게 한다. 파서·룰엔진은 순수 함수로 작성해 Node `assert`로 브라우저 없이 단위 테스트한다. DOM 렌더링/이벤트는 브라우저에서 수동 확인.

**Tech Stack:** Vanilla HTML/CSS/JS (ES modules 미사용, 단순 `<script>` 순서 로딩), Google Fonts(Outfit + Noto Sans KR), `localStorage`. 테스트는 Node 내장 `assert` (프레임워크 의존성 추가 안 함).

---

## File Structure

```
알릴의무 프로젝트/
  index.html        nav + hero + Step1~3 섹션 마크업, <script> 로딩 순서: data.js → app.js
  style.css         insight.html 톤 차용 전용 스타일 (Tailwind 미사용)
  data.js           DISEASE_11(11대질병 ICD 매핑), Q_DEFS(Q1~Q5 정의/트리거 텍스트) 상수만. DOM 접근 없음.
  app.js            parseHistoryText(), classifyHistories(), 렌더링 함수, 이벤트 바인딩, localStorage 헬퍼
  test/parser.test.js     parseHistoryText() 단위 테스트 (Node assert, 브라우저 의존 없음)
  test/classify.test.js   classifyHistories() 단위 테스트 (Node assert)
```

`app.js`는 순수 로직 함수(`parseHistoryText`, `classifyHistories`)와 DOM 의존 함수를 분리한다 — 순수 함수는 파일 상단, `module.exports`로 Node 테스트에서도 가져다 쓸 수 있게 하고(브라우저에서는 `module`이 없으므로 `typeof module !== 'undefined'` 가드로 감싼다), DOM 함수는 그 아래.

---

## Task 1: 폴더 스캐폴드 + 정적 셸(nav/hero)

**Files:**
- Create: `알릴의무 프로젝트/index.html`
- Create: `알릴의무 프로젝트/style.css`

- [ ] **Step 1: 폴더와 빈 파일 생성**

```bash
mkdir -p "알릴의무 프로젝트"
touch "알릴의무 프로젝트/index.html" "알릴의무 프로젝트/style.css" "알릴의무 프로젝트/data.js" "알릴의무 프로젝트/app.js"
```

- [ ] **Step 2: `index.html`에 head + nav + hero 작성**

```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>알릴의무 작성 보조 도구</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&family=Noto+Sans+KR:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <nav id="al-nav">
    <div class="nav-inner">
      <a href="#" class="nav-logo">알릴의무 도우미</a>
      <a href="../index.html" class="nav-back">← 가입제안서PJ로 돌아가기</a>
    </div>
  </nav>

  <main id="al-content">
    <section class="hero">
      <span class="hero-tag">표준알릴의무 작성 보조</span>
      <h1 class="hero-title">알릴의무, 자동으로<br>분류해보세요</h1>
      <p class="hero-sub">병력을 입력하면 표준알릴의무 5문항(Q1~Q5)으로 자동 분류합니다. 이 도구는 작성을 대체하지 않고 보조합니다 — 최종 청약서 기재 전 반드시 직접 확인하세요.</p>
      <div class="hero-badges">
        <span class="hero-badge">Q1 3개월 진찰/검사</span>
        <span class="hero-badge">Q2 상시복용</span>
        <span class="hero-badge">Q3 1년 재검사</span>
        <span class="hero-badge">Q4 5년 입원/수술/치료</span>
        <span class="hero-badge">Q5 11대 질병</span>
      </div>
      <div class="ce-callout warning">
        <p>자동분류 결과는 항상 "초안"입니다. 최종 청약서 기재 전 직접 확인이 필수입니다.</p>
      </div>
    </section>
  </main>

  <script src="data.js"></script>
  <script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 3: `style.css`에 insight.html 톤 기반 기본 레이아웃 작성**

```css
:root {
  --navy-dark: #071440;
  --navy: #1A3A8F;
  --bg: #fff;
  --text: #111;
  --text-muted: #6b7280;
  --text-faint: #9ca3af;
  --card-bg: #fafafa;
  --card-border: #efefef;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body {
  font-family: 'Outfit', 'Noto Sans KR', sans-serif;
  background: var(--bg);
  color: var(--text);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

#al-nav {
  position: sticky; top: 0; z-index: 50;
  background: rgba(255,255,255,0.88);
  backdrop-filter: blur(14px); -webkit-backdrop-filter: blur(14px);
  border-bottom: 1px solid #f0f0f0;
}
.nav-inner {
  max-width: 860px; margin: 0 auto;
  display: flex; align-items: center; justify-content: space-between;
  padding: 16px 24px;
}
.nav-logo { font-size: 0.95rem; font-weight: 800; color: var(--text); text-decoration: none; letter-spacing: -0.5px; }
.nav-back { font-size: 0.82rem; font-weight: 600; color: var(--text-faint); text-decoration: none; transition: color 0.2s; }
.nav-back:hover { color: var(--text); }

#al-content { max-width: 860px; margin: 0 auto; padding: 72px 24px 160px; }

.hero-tag {
  display: inline-block; font-size: 0.68rem; font-weight: 700;
  letter-spacing: 2px; text-transform: uppercase; color: var(--text-faint); margin-bottom: 20px;
}
.hero-title {
  font-size: clamp(2.2rem, 5.5vw, 3.4rem); font-weight: 900;
  letter-spacing: -2px; line-height: 1.15; color: var(--text); margin-bottom: 24px;
}
.hero-sub { font-size: 1rem; color: var(--text-muted); max-width: 560px; line-height: 1.8; margin-bottom: 28px; }
.hero-badges { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 28px; }
.hero-badge { font-size: 0.75rem; font-weight: 600; color: #374151; background: #f3f4f6; border-radius: 8px; padding: 5px 12px; }

.ce-callout { border-radius: 12px; padding: 14px 16px; }
.ce-callout.warning { background: #fffbeb; border: 1px solid #fde68a; color: #92400e; font-size: 0.85rem; }

.section { margin-top: 80px; }
.eyebrow {
  font-size: 0.65rem; font-weight: 700; letter-spacing: 2.5px;
  text-transform: uppercase; color: #c4c4c4; margin-bottom: 10px; display: block;
}
h2.stitle {
  font-size: clamp(1.4rem, 2.8vw, 1.9rem); font-weight: 800;
  letter-spacing: -0.5px; color: var(--text); margin-bottom: 12px; line-height: 1.2;
}
.slead { font-size: 0.9rem; color: var(--text-muted); line-height: 1.8; margin-bottom: 28px; max-width: 560px; }

@media (max-width: 640px) {
  #al-content { padding: 48px 16px 120px; }
  .hero-badges { gap: 6px; }
}
```

- [ ] **Step 4: 브라우저로 열어 nav/hero가 깨지지 않는지 확인**

`알릴의무 프로젝트/index.html`을 더블클릭해 브라우저로 열고, nav와 hero 텍스트/배지/경고박스가 보이는지 눈으로 확인. 자동화 테스트 대상 아님(순수 마크업).

- [ ] **Step 5: Commit**

```bash
git add "알릴의무 프로젝트/index.html" "알릴의무 프로젝트/style.css"
git commit -m "feat(알릴의무): 페이지 셸(nav/hero) 추가"
```

---

## Task 2: data.js — Q1~Q5 정의 + 11대 질병 ICD 매핑

**Files:**
- Create: `알릴의무 프로젝트/data.js`

- [ ] **Step 1: `data.js` 작성**

```javascript
// 표준알릴의무 5문항 정의 (README 2장 기준)
const Q_DEFS = [
  { id: 'Q1', months: 3, title: 'Q1. 최근 3개월 진찰·검사', trigger: '진찰/검사(건강검진포함) - 확정진단/의심소견/치료/입원/수술/투약' },
  { id: 'Q2', months: 3, title: 'Q2. 상시복용 약물', trigger: '마약·혈압강하제·신경안정제·수면제·각성제·진통제 상시복용' },
  { id: 'Q3', months: 12, title: 'Q3. 최근 1년 추가검사(재검사)', trigger: '재검사(추가검사) - 진단 확정 여부와 무관' },
  { id: 'Q4', months: 60, title: 'Q4. 최근 5년 입원·수술·장기치료', trigger: '입원/수술/계속 7일↑ 치료/계속 30일↑ 투약 (질병종류 무관)' },
  { id: 'Q5', months: 60, title: 'Q5. 최근 5년 11대 질병', trigger: '11대 질병이면 단발성 진료라도 해당 (계속성 조건 없음)' },
];

// 11대 질병 ICD-10 코드 prefix 매핑 (점 없이 "K291" 형태 기준, README 7-2 보수적 매핑 한계 있음)
const DISEASE_11 = {
  '암': ['C'],
  '백혈병': ['C91', 'C92', 'C93', 'C94', 'C95'],
  '고혈압': ['I10', 'I11', 'I12', 'I13', 'I15'],
  '협심증': ['I20'],
  '심근경색': ['I21', 'I22'],
  '심장판막증': ['I05', 'I06', 'I07', 'I08', 'I34', 'I35', 'I36', 'I37'],
  '간경화증': ['K74'],
  '뇌졸중': ['I60', 'I61', 'I62', 'I63', 'I64'],
  '당뇨병': ['E10', 'E11', 'E12', 'E13', 'E14'],
  '에이즈': ['B20', 'B21', 'B22', 'B23', 'B24'],
};

if (typeof module !== 'undefined') {
  module.exports = { Q_DEFS, DISEASE_11 };
}
```

- [ ] **Step 2: Commit**

```bash
git add "알릴의무 프로젝트/data.js"
git commit -m "feat(알릴의무): Q1~Q5 정의 및 11대질병 ICD 매핑 데이터 추가"
```

---

## Task 3: app.js — `parseHistoryText()` 파서 (TDD)

**Files:**
- Create: `알릴의무 프로젝트/test/parser.test.js`
- Create: `알릴의무 프로젝트/app.js`

파서는 README 5장 규칙을 따른다: `🔴진단명 .../코드`로 시작하는 블록을 한 건의 병력으로 인식하고, 이어지는 `입원:`, `수술:`, `치료/현상태:` 줄에서 정보를 추출한다. "~"로 끝나는 날짜는 자동으로 오늘 날짜를 채우지 않는다(README 5장에 기록된 과거 버그 재발 방지).

- [ ] **Step 1: 실패하는 테스트 작성**

`알릴의무 프로젝트/test/parser.test.js`:

```javascript
const assert = require('assert');
const { parseHistoryText } = require('../app.js');

// 기본 블록 파싱: 진단명/코드/입원/수술/현상태 추출
{
  const input = `🔴고혈압/I10
입원: 없음
수술: 없음
치료/현상태: 24.01.05 진단, 현재 약물치료중`;
  const result = parseHistoryText(input);
  assert.strictEqual(result.length, 1, '병력 1건이 인식되어야 함');
  assert.strictEqual(result[0].진단명, '고혈압');
  assert.strictEqual(result[0].진단코드, 'I10');
  assert.strictEqual(result[0].입원여부, false);
  assert.strictEqual(result[0].수술여부, false);
  assert.strictEqual(result[0].현재상태, '24.01.05 진단, 현재 약물치료중');
  console.log('PASS: 기본 블록 파싱');
}

// "~"로 끝나는 날짜는 최근진료일을 자동으로 오늘로 채우지 않음 (README 5장 버그 재발 방지)
{
  const input = `🔴협심증/I20
입원: 없음
수술: 없음
치료/현상태: 22.03.10~`;
  const result = parseHistoryText(input);
  assert.strictEqual(result[0].최근진료일, null, '"~"로 끝나는 표기는 최근진료일을 비워둬야 함(자동완치/진행중 추정 금지)');
  console.log('PASS: 미완결 날짜("~") 자동완성 금지');
}

// 입원/수술 일수·명 추출
{
  const input = `🔴급성충수염/K35
입원: 7일
수술: 충수절제술
치료/현상태: 25.02.01 수술 후 완치`;
  const result = parseHistoryText(input);
  assert.strictEqual(result[0].입원여부, true);
  assert.strictEqual(result[0].입원일수, 7);
  assert.strictEqual(result[0].수술여부, true);
  assert.strictEqual(result[0].수술명, '충수절제술');
  console.log('PASS: 입원/수술 정보 추출');
}

// 여러 블록(여러 병력) 동시 인식
{
  const input = `🔴고혈압/I10
입원: 없음
수술: 없음
치료/현상태: 24.01.05 진단

🔴당뇨병/E11
입원: 없음
수술: 없음
치료/현상태: 23.05.10 진단`;
  const result = parseHistoryText(input);
  assert.strictEqual(result.length, 2, '두 블록이 각각 병력으로 인식되어야 함');
  console.log('PASS: 다중 블록 파싱');
}

// 빈 입력은 빈 배열
{
  const result = parseHistoryText('');
  assert.strictEqual(result.length, 0);
  console.log('PASS: 빈 입력 처리');
}

console.log('전체 통과: parser.test.js');
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `node "알릴의무 프로젝트/test/parser.test.js"`
Expected: `app.js`가 비어있어 `Cannot find module '../app.js'` 또는 `parseHistoryText is not a function` 에러로 FAIL

- [ ] **Step 3: `parseHistoryText()` 구현**

`알릴의무 프로젝트/app.js` (파일 최상단에 작성):

```javascript
function parseHistoryText(text) {
  if (!text || !text.trim()) return [];

  const blocks = text.split(/\n\s*\n/).map(b => b.trim()).filter(Boolean);
  const results = [];

  for (const block of blocks) {
    const lines = block.split('\n').map(l => l.trim()).filter(Boolean);
    const headerLine = lines.find(l => l.startsWith('🔴'));
    if (!headerLine) continue;

    const headerBody = headerLine.replace('🔴', '').trim();
    const [namePart, codePart] = headerBody.split('/').map(s => (s || '').trim());

    const entry = {
      진단명: namePart || '',
      진단코드: codePart || '',
      최초진단일: null,
      최근진료일: null,
      입원여부: false,
      입원일수: null,
      수술여부: false,
      수술명: null,
      계속치료일수: null,
      계속투약일수: null,
      재검사여부: false,
      상시복용여부: false,
      현재상태: '',
      비고: '',
      원본: block,
    };

    for (const line of lines) {
      if (line.startsWith('입원:')) {
        const v = line.replace('입원:', '').trim();
        if (v && v !== '없음') {
          entry.입원여부 = true;
          const m = v.match(/(\d+)\s*일/);
          if (m) entry.입원일수 = parseInt(m[1], 10);
        }
      } else if (line.startsWith('수술:')) {
        const v = line.replace('수술:', '').trim();
        if (v && v !== '없음') {
          entry.수술여부 = true;
          entry.수술명 = v;
        }
      } else if (line.startsWith('치료/현상태:')) {
        const v = line.replace('치료/현상태:', '').trim();
        entry.현재상태 = v;
        // "~"로 끝나면 진행중 표기이지만 최근진료일을 자동으로 채우지 않는다
        // (README 5장: 과거에 오늘 날짜로 자동완성 → 완치 사례 오분류 버그 발생)
        if (!v.endsWith('~')) {
          const dateMatch = v.match(/(\d{2})\.(\d{2})\.(\d{2})/);
          if (dateMatch) {
            entry.최근진료일 = `20${dateMatch[1]}-${dateMatch[2]}-${dateMatch[3]}`;
            entry.최초진단일 = entry.최초진단일 || entry.최근진료일;
          }
        }
      }
    }

    results.push(entry);
  }

  return results;
}

if (typeof module !== 'undefined') {
  module.exports = { parseHistoryText };
}
```

- [ ] **Step 4: 테스트 실행해서 통과 확인**

Run: `node "알릴의무 프로젝트/test/parser.test.js"`
Expected: 5개 `PASS:` 라인 출력 후 `전체 통과: parser.test.js`, exit code 0

- [ ] **Step 5: Commit**

```bash
git add "알릴의무 프로젝트/app.js" "알릴의무 프로젝트/test/parser.test.js"
git commit -m "feat(알릴의무): parseHistoryText 파서 구현 및 단위테스트 추가"
```

---

## Task 4: app.js — `classifyHistories()` 룰엔진 (TDD)

**Files:**
- Create: `알릴의무 프로젝트/test/classify.test.js`
- Modify: `알릴의무 프로젝트/app.js`

룰엔진은 README 6장 원칙을 따른다: Q2는 상시복용 체크 여부 자체를 신뢰(날짜 계산 안 함), Q4는 계속성 조건 있음, Q5는 11대질병이면 계속성 없이도 해당, 날짜 정보 불충분 시 무조건 "확인필요"로 표시하고 자동 INCLUDE 하지 않는다.

이 테스트는 `data.js`의 `Q_DEFS`/`DISEASE_11`을 사용하므로 Node에서 `global`에 주입해서 로드한다 (브라우저에서는 `<script>` 순서 로딩으로 전역에 이미 존재).

- [ ] **Step 1: 실패하는 테스트 작성**

`알릴의무 프로젝트/test/classify.test.js`:

```javascript
const assert = require('assert');
const path = require('path');
const fs = require('fs');

// data.js를 전역(global)에 주입 (app.js가 브라우저 전역 변수처럼 Q_DEFS/DISEASE_11을 참조하므로)
const dataSrc = fs.readFileSync(path.join(__dirname, '..', 'data.js'), 'utf8');
const dataModule = { exports: {} };
new Function('module', 'exports', dataSrc)(dataModule, dataModule.exports);
global.Q_DEFS = dataModule.exports.Q_DEFS;
global.DISEASE_11 = dataModule.exports.DISEASE_11;

const { classifyHistories } = require('../app.js');

const TODAY = '2026-06-25'; // 고정 기준일 (테스트 결정론 확보용, classifyHistories 두번째 인자)

// Q5: 11대 질병(고혈압, I10)은 5년 이내 단발 진료라도 해당, 계속성 조건 없음
{
  const histories = [{
    진단명: '고혈압', 진단코드: 'I10',
    최초진단일: '2023-01-05', 최근진료일: '2023-01-05',
    입원여부: false, 입원일수: null, 수술여부: false, 수술명: null,
    계속치료일수: null, 계속투약일수: null, 재검사여부: false, 상시복용여부: false,
    현재상태: '', 비고: '', 원본: '',
  }];
  const result = classifyHistories(histories, TODAY);
  const q5Items = result.Q5.included;
  assert.strictEqual(q5Items.length, 1, '11대질병은 단발 진료라도 Q5에 포함되어야 함');
  console.log('PASS: Q5 11대질병 단발진료 포함');
}

// Q2: 상시복용 체크가 있으면 날짜와 무관하게 포함 (README 6장: 날짜 계산보다 체크 여부 신뢰)
{
  const histories = [{
    진단명: '고혈압', 진단코드: 'I10',
    최초진단일: '2010-01-01', 최근진료일: '2010-01-01',
    입원여부: false, 입원일수: null, 수술여부: false, 수술명: null,
    계속치료일수: null, 계속투약일수: null, 재검사여부: false, 상시복용여부: true,
    현재상태: '', 비고: '', 원본: '',
  }];
  const result = classifyHistories(histories, TODAY);
  assert.strictEqual(result.Q2.included.length, 1, '상시복용 체크가 있으면 최초진단일이 오래돼도 Q2 포함');
  console.log('PASS: Q2 상시복용 체크 우선');
}

// Q1: 최근진료일이 3개월 이내가 아니면 미포함
{
  const histories = [{
    진단명: '감기', 진단코드: 'J00',
    최초진단일: '2024-01-01', 최근진료일: '2024-01-01',
    입원여부: false, 입원일수: null, 수술여부: false, 수술명: null,
    계속치료일수: null, 계속투약일수: null, 재검사여부: false, 상시복용여부: false,
    현재상태: '', 비고: '', 원본: '',
  }];
  const result = classifyHistories(histories, TODAY);
  assert.strictEqual(result.Q1.included.length, 0);
  console.log('PASS: Q1 3개월 초과 시 미포함');
}

// 날짜 정보 불충분(최근진료일 null) → 자동 INCLUDE 금지, "확인필요"로 분리
{
  const histories = [{
    진단명: '협심증', 진단코드: 'I20',
    최초진단일: null, 최근진료일: null,
    입원여부: false, 입원일수: null, 수술여부: false, 수술명: null,
    계속치료일수: null, 계속투약일수: null, 재검사여부: false, 상시복용여부: false,
    현재상태: '', 비고: '', 원본: '',
  }];
  const result = classifyHistories(histories, TODAY);
  assert.strictEqual(result.Q1.included.length, 0, '날짜 불충분 항목은 Q1.included에 자동 포함되면 안 됨');
  assert.strictEqual(result.Q1.review.length, 1, '날짜 불충분 항목은 Q1.review(확인필요)에 들어가야 함');
  console.log('PASS: 날짜 불충분 시 확인필요 처리, 자동포함 금지');
}

// Q4: 계속 7일 이상 입원이면 5년 이내 포함, 단발 입원(7일 미만)은 11대질병이 아니면 미포함
{
  const histories = [{
    진단명: '폐렴', 진단코드: 'J18',
    최초진단일: '2025-01-01', 최근진료일: '2025-01-01',
    입원여부: true, 입원일수: 10, 수술여부: false, 수술명: null,
    계속치료일수: null, 계속투약일수: null, 재검사여부: false, 상시복용여부: false,
    현재상태: '', 비고: '', 원본: '',
  }];
  const result = classifyHistories(histories, TODAY);
  assert.strictEqual(result.Q4.included.length, 1, '7일 이상 입원은 Q4 포함');
  console.log('PASS: Q4 7일 이상 입원 포함');
}

console.log('전체 통과: classify.test.js');
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `node "알릴의무 프로젝트/test/classify.test.js"`
Expected: `classifyHistories is not a function` 으로 FAIL

- [ ] **Step 3: `classifyHistories()` 구현**

`알릴의무 프로젝트/app.js`에 `parseHistoryText` 아래에 추가:

```javascript
function daysBetween(dateStr, todayStr) {
  if (!dateStr) return null;
  const d = new Date(dateStr);
  const today = new Date(todayStr);
  return Math.floor((today - d) / (1000 * 60 * 60 * 24));
}

function matchesDisease11(code) {
  if (!code) return false;
  for (const prefixes of Object.values(DISEASE_11)) {
    if (prefixes.some(p => code.startsWith(p))) return true;
  }
  return false;
}

function withinMonths(dateStr, todayStr, months) {
  const days = daysBetween(dateStr, todayStr);
  if (days === null) return null; // 정보 불충분
  return days <= months * 30.44 && days >= 0;
}

function classifyHistories(histories, todayStr) {
  const result = {};
  for (const q of Q_DEFS) {
    result[q.id] = { included: [], review: [] };
  }

  for (const h of histories) {
    // Q1: 최근 3개월 진찰/검사
    {
      const within = withinMonths(h.최근진료일, todayStr, 3);
      if (within === null) result.Q1.review.push(h);
      else if (within) result.Q1.included.push(h);
    }

    // Q2: 상시복용 - 날짜 계산보다 체크 여부 신뢰 (README 6장)
    if (h.상시복용여부) {
      result.Q2.included.push(h);
    }

    // Q3: 최근 1년 재검사 - 체크 여부로만 판단(원문 자동추출은 1차 범위 제외)
    if (h.재검사여부) {
      const within = withinMonths(h.최근진료일, todayStr, 12);
      if (within === null) result.Q3.review.push(h);
      else if (within) result.Q3.included.push(h);
    }

    // Q4: 5년 이내 입원/수술/계속 7일↑치료/계속 30일↑투약, 질병종류 무관
    {
      const within = withinMonths(h.최근진료일, todayStr, 60);
      const hasQ4Trigger =
        h.입원여부 ||
        h.수술여부 ||
        (h.계속치료일수 !== null && h.계속치료일수 >= 7) ||
        (h.계속투약일수 !== null && h.계속투약일수 >= 30) ||
        (h.입원일수 !== null && h.입원일수 >= 7);
      if (hasQ4Trigger) {
        if (within === null) result.Q4.review.push(h);
        else if (within) result.Q4.included.push(h);
      }
    }

    // Q5: 11대 질병이면 계속성 조건 없이 5년 이내 단발 진료도 포함
    if (matchesDisease11(h.진단코드)) {
      const within = withinMonths(h.최근진료일, todayStr, 60);
      if (within === null) result.Q5.review.push(h);
      else if (within) result.Q5.included.push(h);
    }
  }

  return result;
}

if (typeof module !== 'undefined') {
  module.exports.classifyHistories = classifyHistories;
}
```

- [ ] **Step 4: 테스트 실행해서 통과 확인**

Run: `node "알릴의무 프로젝트/test/classify.test.js"`
Expected: 5개 `PASS:` 라인과 `전체 통과: classify.test.js`, exit code 0

- [ ] **Step 5: 파서 테스트도 다시 실행해 회귀 없는지 확인**

Run: `node "알릴의무 프로젝트/test/parser.test.js"`
Expected: 여전히 전체 통과

- [ ] **Step 6: Commit**

```bash
git add "알릴의무 프로젝트/app.js" "알릴의무 프로젝트/test/classify.test.js"
git commit -m "feat(알릴의무): classifyHistories Q1~Q5 룰엔진 구현 및 단위테스트 추가"
```

---

## Task 5: Step 1 입력 섹션 UI + 이벤트

**Files:**
- Modify: `알릴의무 프로젝트/index.html`
- Modify: `알릴의무 프로젝트/style.css`
- Modify: `알릴의무 프로젝트/app.js`

- [ ] **Step 1: `index.html`의 `<section class="hero">` 다음에 Step1 섹션 추가**

```html
<section class="section" id="step1">
  <span class="eyebrow">STEP 1 · 병력 입력</span>
  <h2 class="stitle">병력을 자유롭게 적어주세요</h2>
  <p class="slead">예시 형식: 🔴진단명/코드 줄 다음 입원/수술/치료·현상태를 적어주세요.</p>
  <textarea id="history-input" class="history-textarea" placeholder="🔴고혈압/I10
입원: 없음
수술: 없음
치료/현상태: 24.01.05 진단, 현재 약물치료중"></textarea>
  <button id="parse-btn" class="btn-primary">표로 변환</button>
</section>
```

- [ ] **Step 2: `style.css`에 textarea/버튼 스타일 추가**

```css
.history-textarea {
  width: 100%; min-height: 180px; padding: 16px;
  background: var(--card-bg); border: 1px solid var(--card-border);
  border-radius: 16px; font-family: inherit; font-size: 0.9rem; color: var(--text);
  resize: vertical; margin-bottom: 16px;
}
.history-textarea:focus { outline: none; border-color: var(--navy); }

.btn-primary {
  background: var(--navy); color: #fff; font-weight: 700; font-size: 0.85rem;
  border: none; border-radius: 99px; padding: 10px 24px; cursor: pointer;
  transition: background 0.2s;
}
.btn-primary:hover { background: var(--navy-dark); }
```

- [ ] **Step 3: `app.js` 맨 아래에 DOM 이벤트 바인딩 추가**

기존 `parseHistoryText`/`classifyHistories` 아래, 파일 끝에 추가 (브라우저에서만 실행되도록 `document` 존재 체크):

```javascript
if (typeof document !== 'undefined') {
  let histories = [];

  function renderAll() {
    // Task 6/7에서 병력표/결과 렌더링 함수가 여기서 호출된다.
  }

  document.getElementById('parse-btn').addEventListener('click', () => {
    const text = document.getElementById('history-input').value;
    const parsed = parseHistoryText(text);
    if (parsed.length === 0) {
      showToast('인식된 병력이 없습니다. 직접 행을 추가해주세요');
      histories.push(emptyHistoryRow());
    } else {
      histories = histories.concat(parsed);
    }
    renderAll();
  });

  function emptyHistoryRow() {
    return {
      진단명: '', 진단코드: '', 최초진단일: null, 최근진료일: null,
      입원여부: false, 입원일수: null, 수술여부: false, 수술명: null,
      계속치료일수: null, 계속투약일수: null, 재검사여부: false, 상시복용여부: false,
      현재상태: '', 비고: '', 원본: '',
    };
  }

  function showToast(message) {
    let toast = document.getElementById('al-toast');
    if (!toast) {
      toast = document.createElement('div');
      toast.id = 'al-toast';
      toast.className = 'toast';
      document.body.appendChild(toast);
    }
    toast.textContent = message;
    toast.classList.add('show');
    setTimeout(() => toast.classList.remove('show'), 2500);
  }
}
```

- [ ] **Step 4: `style.css`에 toast 스타일 추가**

```css
.toast {
  position: fixed; bottom: 24px; left: 50%; transform: translate(-50%, 100px);
  background: var(--navy-dark); color: #fff; padding: 12px 20px; border-radius: 12px;
  font-size: 0.85rem; opacity: 0; transition: all 0.4s cubic-bezier(0.22, 1, 0.36, 1);
  box-shadow: 0 10px 40px rgba(0,0,0,0.2); z-index: 100;
}
.toast.show { transform: translate(-50%, 0); opacity: 1; }
```

- [ ] **Step 5: 브라우저에서 빈 입력으로 "표로 변환" 클릭 → 토스트가 뜨는지 확인**

`index.html`을 브라우저로 열고 textarea를 비운 채 버튼 클릭. "인식된 병력이 없습니다..." 토스트가 2.5초간 나타나는지 확인.

- [ ] **Step 6: 예시 텍스트로 "표로 변환" 클릭 → 콘솔에서 histories 배열 확인**

브라우저 개발자도구 콘솔에서 `console.log(histories)`를 임시로 찍어보거나, placeholder 예시를 textarea에 그대로 입력 후 변환 클릭, 개발자도구에서 에러 없이 실행되는지 확인 (병력표 렌더링은 Task 6에서 추가되므로 화면에는 아직 안 보임).

- [ ] **Step 7: 단위 테스트 회귀 확인**

Run: `node "알릴의무 프로젝트/test/parser.test.js" && node "알릴의무 프로젝트/test/classify.test.js"`
Expected: 둘 다 전체 통과

- [ ] **Step 8: Commit**

```bash
git add "알릴의무 프로젝트/index.html" "알릴의무 프로젝트/style.css" "알릴의무 프로젝트/app.js"
git commit -m "feat(알릴의무): Step1 입력 UI 및 변환 버튼 이벤트 추가"
```

---

## Task 6: Step 2 병력표(카드 리스트) 렌더링 + 편집

**Files:**
- Modify: `알릴의무 프로젝트/index.html`
- Modify: `알릴의무 프로젝트/style.css`
- Modify: `알릴의무 프로젝트/app.js`

- [ ] **Step 1: `index.html`의 Step1 섹션 다음에 Step2 섹션 추가**

```html
<section class="section" id="step2">
  <span class="eyebrow">STEP 2 · 병력 확인 및 수정</span>
  <h2 class="stitle">자동 인식 결과입니다</h2>
  <p class="slead">날짜·일수는 직접 확인해주세요.</p>
  <div id="history-list" class="hier-box"></div>
  <button id="add-row-btn" class="btn-secondary">+ 병력 추가</button>
</section>
```

- [ ] **Step 2: `style.css`에 카드 리스트/필드 스타일 추가**

```css
.hier-box { background: var(--card-bg); border: 1px solid var(--card-border); border-radius: 16px; padding: 8px; margin-bottom: 16px; }
.history-card { padding: 20px; border-bottom: 1px solid #ececec; }
.history-card:last-child { border-bottom: none; }
.history-card-head { display: flex; align-items: center; justify-content: space-between; margin-bottom: 12px; }
.history-card-title { font-weight: 700; font-size: 0.95rem; }
.review-badge { font-size: 0.6rem; font-weight: 700; padding: 3px 9px; border-radius: 99px; background: #fff7ed; color: #c2410c; text-transform: uppercase; letter-spacing: 1px; }
.history-fields { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; }
.history-fields label { display: flex; flex-direction: column; font-size: 0.72rem; color: var(--text-faint); gap: 4px; }
.history-fields input, .history-fields select { font-size: 0.85rem; padding: 6px 8px; border: 1px solid var(--card-border); border-radius: 8px; font-family: inherit; }
.history-card-actions { margin-top: 10px; text-align: right; }
.btn-link-danger { background: none; border: none; color: #c2410c; font-size: 0.78rem; cursor: pointer; }
.btn-secondary { background: #fff; border: 1px solid var(--card-border); color: var(--text); font-weight: 600; font-size: 0.85rem; border-radius: 99px; padding: 8px 18px; cursor: pointer; }

@media (max-width: 640px) {
  .history-fields { grid-template-columns: 1fr 1fr; }
}
```

- [ ] **Step 3: `app.js`의 `renderAll()` 안에 병력표 렌더링 함수 호출 추가, 함수 정의**

`renderAll()` 함수 본문을 다음으로 교체하고, 바로 아래에 `renderHistoryList`, 필드 정의 배열을 추가:

```javascript
  const HISTORY_FIELDS = [
    { key: '진단명', label: '진단명', type: 'text' },
    { key: '진단코드', label: '진단코드', type: 'text' },
    { key: '최초진단일', label: '최초진단일', type: 'date' },
    { key: '최근진료일', label: '최근진료일', type: 'date' },
    { key: '입원일수', label: '입원일수', type: 'number' },
    { key: '수술명', label: '수술명', type: 'text' },
    { key: '계속치료일수', label: '계속치료일수', type: 'number' },
    { key: '계속투약일수', label: '계속투약일수', type: 'number' },
    { key: '비고', label: '비고', type: 'text' },
  ];

  function isReviewNeeded(h) {
    return !h.최근진료일;
  }

  function renderHistoryList() {
    const container = document.getElementById('history-list');
    container.innerHTML = '';
    histories.forEach((h, idx) => {
      const card = document.createElement('div');
      card.className = 'history-card';

      const head = document.createElement('div');
      head.className = 'history-card-head';
      const title = document.createElement('span');
      title.className = 'history-card-title';
      title.textContent = h.진단명 || `병력 ${idx + 1}`;
      head.appendChild(title);
      if (isReviewNeeded(h)) {
        const badge = document.createElement('span');
        badge.className = 'review-badge';
        badge.textContent = '확인필요';
        head.appendChild(badge);
      }
      card.appendChild(head);

      const fields = document.createElement('div');
      fields.className = 'history-fields';
      HISTORY_FIELDS.forEach(f => {
        const label = document.createElement('label');
        label.textContent = f.label;
        const input = document.createElement('input');
        input.type = f.type;
        input.value = h[f.key] === null || h[f.key] === undefined ? '' : h[f.key];
        input.addEventListener('change', () => {
          h[f.key] = f.type === 'number'
            ? (input.value === '' ? null : Number(input.value))
            : (input.value === '' ? (f.type === 'date' ? null : '') : input.value);
          renderAll();
        });
        label.appendChild(input);
        fields.appendChild(label);
      });
      card.appendChild(fields);

      ['입원여부', '수술여부', '재검사여부', '상시복용여부'].forEach(key => {
        const label = document.createElement('label');
        label.style.flexDirection = 'row';
        label.style.alignItems = 'center';
        label.style.gap = '6px';
        const cb = document.createElement('input');
        cb.type = 'checkbox';
        cb.checked = !!h[key];
        cb.addEventListener('change', () => { h[key] = cb.checked; renderAll(); });
        label.appendChild(cb);
        label.appendChild(document.createTextNode(key));
        fields.appendChild(label);
      });

      const actions = document.createElement('div');
      actions.className = 'history-card-actions';
      const delBtn = document.createElement('button');
      delBtn.className = 'btn-link-danger';
      delBtn.textContent = '삭제';
      delBtn.addEventListener('click', () => { histories.splice(idx, 1); renderAll(); });
      actions.appendChild(delBtn);
      card.appendChild(actions);

      container.appendChild(card);
    });
  }

  function renderAll() {
    renderHistoryList();
  }

  document.getElementById('add-row-btn').addEventListener('click', () => {
    histories.push(emptyHistoryRow());
    renderAll();
  });
```

`renderAll`이 함수 선언으로 두 번 정의되지 않도록, Step 1에서 추가한 빈 `function renderAll() { ... }`을 이 새 버전으로 **교체**한다(중복 선언 금지).

- [ ] **Step 4: 브라우저에서 확인**

1. textarea에 예시 두 블록(고혈압/I10, 당뇨병/E11) 입력 후 "표로 변환" 클릭
2. 카드 2개가 렌더링되는지 확인, 최근진료일이 비어있으면 "확인필요" 배지가 보이는지 확인
3. 진단명 input을 수정하면 카드 제목이 즉시 바뀌는지 확인
4. "삭제" 클릭 시 카드가 사라지는지 확인
5. "+ 병력 추가" 클릭 시 빈 카드가 추가되는지 확인

- [ ] **Step 5: 단위 테스트 회귀 확인**

Run: `node "알릴의무 프로젝트/test/parser.test.js" && node "알릴의무 프로젝트/test/classify.test.js"`
Expected: 둘 다 전체 통과 (DOM 코드는 `typeof document` 가드로 Node 실행 시 건너뜀)

- [ ] **Step 6: Commit**

```bash
git add "알릴의무 프로젝트/index.html" "알릴의무 프로젝트/style.css" "알릴의무 프로젝트/app.js"
git commit -m "feat(알릴의무): Step2 병력표 카드 리스트 렌더링/편집/삭제 구현"
```

---

## Task 7: Step 3 분류결과 렌더링 + 체크박스 확정 + 복사

**Files:**
- Modify: `알릴의무 프로젝트/index.html`
- Modify: `알릴의무 프로젝트/style.css`
- Modify: `알릴의무 프로젝트/app.js`

- [ ] **Step 1: `index.html`의 Step2 섹션 다음에 Step3 섹션 추가**

```html
<section class="section" id="step3">
  <span class="eyebrow">STEP 3 · 표준알릴의무 분류결과</span>
  <h2 class="stitle">문항별 분류결과</h2>
  <div id="classify-result"></div>
  <button id="copy-btn" class="btn-primary copy-sticky">결과 복사</button>
</section>
```

- [ ] **Step 2: `style.css`에 분류결과/배지/sticky 버튼 스타일 추가**

```css
.q-block { padding: 28px 0; border-bottom: 1px solid #f3f4f6; }
.q-block:last-child { border-bottom: none; }
.q-title { font-size: 1.05rem; font-weight: 800; margin-bottom: 4px; }
.q-oneliner { font-size: 0.8rem; color: var(--text-faint); margin-bottom: 14px; }
.q-item-row { display: flex; align-items: center; gap: 8px; padding: 6px 0; font-size: 0.85rem; }
.q-empty { font-size: 0.82rem; color: var(--text-faint); padding: 6px 0; }
.cat-badge { font-size: 0.6rem; font-weight: 700; padding: 2px 8px; border-radius: 99px; text-transform: uppercase; }
.cat-s { background: #eff6ff; color: #1d4ed8; }
.cat-r { background: #fff7ed; color: #c2410c; }
.copy-sticky { position: sticky; bottom: 16px; display: block; margin: 0 auto; }
```

- [ ] **Step 3: `app.js`의 `renderAll()`에 결과 렌더링 함수 추가 + 복사 이벤트**

`renderAll()` 본문에 `renderHistoryList();` 다음 줄로 `renderClassifyResult();` 추가하고, 그 아래 함수들을 정의:

```javascript
  const TODAY_ISO = new Date().toISOString().slice(0, 10);
  const checkedKeys = new Set(); // `${qId}::${historyIndex}` 형태로 체크 상태 저장

  function renderClassifyResult() {
    const container = document.getElementById('classify-result');
    container.innerHTML = '';
    const result = classifyHistories(histories, TODAY_ISO);

    Q_DEFS.forEach(q => {
      const block = document.createElement('div');
      block.className = 'q-block';

      const title = document.createElement('div');
      title.className = 'q-title';
      title.textContent = q.title;
      block.appendChild(title);

      const oneliner = document.createElement('div');
      oneliner.className = 'q-oneliner';
      oneliner.textContent = q.trigger;
      block.appendChild(oneliner);

      const included = result[q.id].included;
      const review = result[q.id].review;

      if (included.length === 0 && review.length === 0) {
        const empty = document.createElement('div');
        empty.className = 'q-empty';
        empty.textContent = '해당 항목 없음';
        block.appendChild(empty);
      }

      [...included.map(h => ({ h, cat: 's' })), ...review.map(h => ({ h, cat: 'r' }))].forEach(({ h, cat }) => {
        const idx = histories.indexOf(h);
        const row = document.createElement('label');
        row.className = 'q-item-row';
        const cb = document.createElement('input');
        cb.type = 'checkbox';
        const key = `${q.id}::${idx}`;
        cb.checked = checkedKeys.has(key);
        cb.addEventListener('change', () => {
          if (cb.checked) checkedKeys.add(key); else checkedKeys.delete(key);
        });
        row.appendChild(cb);
        const badge = document.createElement('span');
        badge.className = `cat-badge cat-${cat}`;
        badge.textContent = cat === 'r' ? '확인필요' : '분류완료';
        row.appendChild(badge);
        row.appendChild(document.createTextNode(h.진단명 || '(이름없음)'));
        block.appendChild(row);
      });

      container.appendChild(block);
    });
  }

  document.getElementById('copy-btn').addEventListener('click', () => {
    const result = classifyHistories(histories, TODAY_ISO);
    const lines = [];
    Q_DEFS.forEach(q => {
      const items = [];
      [...result[q.id].included, ...result[q.id].review].forEach(h => {
        const idx = histories.indexOf(h);
        if (checkedKeys.has(`${q.id}::${idx}`)) items.push(h.진단명 || '(이름없음)');
      });
      if (items.length > 0) lines.push(`${q.id}: ${items.join(', ')}`);
    });
    const text = lines.length > 0 ? lines.join('\n') : '체크된 항목이 없습니다.';
    navigator.clipboard.writeText(text).then(() => showToast('결과가 복사되었습니다'));
  });
```

`renderAll()`은 다음과 같이 두 호출을 모두 포함하도록 수정:

```javascript
  function renderAll() {
    renderHistoryList();
    renderClassifyResult();
  }
```

- [ ] **Step 4: 브라우저에서 확인**

1. 예시 병력 입력 후 변환 → Step3에 Q1~Q5 블록이 각각 트리거 설명과 함께 보이는지 확인
2. 11대질병(예: 고혈압/I10, 5년 이내 날짜)을 넣었을 때 Q5에 "분류완료" 배지로 나오는지 확인
3. 날짜를 비운 항목이 "확인필요" 배지로 나오는지 확인
4. 체크박스 체크 후 "결과 복사" 클릭 → 토스트 "결과가 복사되었습니다" 표시 확인, 메모장에 붙여넣어 `Q5: 고혈압` 형식으로 나오는지 확인

- [ ] **Step 5: 단위 테스트 회귀 확인**

Run: `node "알릴의무 프로젝트/test/parser.test.js" && node "알릴의무 프로젝트/test/classify.test.js"`
Expected: 둘 다 전체 통과

- [ ] **Step 6: Commit**

```bash
git add "알릴의무 프로젝트/index.html" "알릴의무 프로젝트/style.css" "알릴의무 프로젝트/app.js"
git commit -m "feat(알릴의무): Step3 분류결과 렌더링, 체크박스 확정, 복사 기능 구현"
```

---

## Task 8: localStorage 자동저장 + 전체 초기화

**Files:**
- Modify: `알릴의무 프로젝트/index.html`
- Modify: `알릴의무 프로젝트/app.js`

- [ ] **Step 1: `index.html`의 nav 안에 초기화 버튼 추가**

`.nav-inner` 안, `nav-back` 링크 앞에:

```html
<button id="reset-all-btn" class="nav-back" style="background:none;border:none;cursor:pointer;">전체 초기화</button>
```

- [ ] **Step 2: `app.js`에 localStorage 저장/복원/초기화 로직 추가**

`let histories = [];` 선언부를 다음으로 교체:

```javascript
  const STORAGE_KEY = 'alil_histories_v1';
  let histories = [];
  try {
    const saved = JSON.parse(localStorage.getItem(STORAGE_KEY) || 'null');
    if (Array.isArray(saved)) histories = saved;
  } catch (e) {
    histories = [];
  }
```

`renderAll()` 끝에 저장 로직 추가:

```javascript
  function renderAll() {
    renderHistoryList();
    renderClassifyResult();
    localStorage.setItem(STORAGE_KEY, JSON.stringify(histories));
  }
```

페이지 로드 시 저장된 값으로 즉시 렌더링하도록 파일 맨 끝(이벤트 바인딩들 이후)에 추가:

```javascript
  renderAll();

  document.getElementById('reset-all-btn').addEventListener('click', () => {
    if (!confirm('입력한 모든 병력과 분류결과를 초기화합니다. 계속할까요?')) return;
    histories = [];
    checkedKeys.clear();
    localStorage.removeItem(STORAGE_KEY);
    renderAll();
    showToast('전체 초기화되었습니다');
  });
```

- [ ] **Step 3: 브라우저에서 확인**

1. 병력 입력 후 변환 → 새로고침(F5) → 카드가 그대로 남아있는지 확인 (자동저장 동작 확인)
2. "전체 초기화" 클릭 → 확인 다이얼로그가 뜨고, 확인 누르면 카드/결과가 모두 비워지고 새로고침해도 비어있는지 확인
3. 확인 다이얼로그에서 취소 누르면 아무 변화 없는지 확인

- [ ] **Step 4: 단위 테스트 회귀 확인**

Run: `node "알릴의무 프로젝트/test/parser.test.js" && node "알릴의무 프로젝트/test/classify.test.js"`
Expected: 둘 다 전체 통과

- [ ] **Step 5: Commit**

```bash
git add "알릴의무 프로젝트/index.html" "알릴의무 프로젝트/app.js"
git commit -m "feat(알릴의무): localStorage 자동저장 및 전체 초기화 기능 추가"
```

---

## Task 9: 모바일 반응형 마무리

**Files:**
- Modify: `알릴의무 프로젝트/style.css`

- [ ] **Step 1: `style.css` 맨 아래 기존 `@media (max-width: 640px)` 블록에 항목 추가**

기존 Task1에서 만든 `@media (max-width: 640px) { ... }` 블록 안에 추가:

```css
  .history-fields { grid-template-columns: 1fr 1fr; }
  .nav-inner { gap: 8px; }
  .nav-back { font-size: 0.72rem; }
  .q-item-row { font-size: 0.8rem; flex-wrap: wrap; }
```

(`.history-fields`는 Task6 미디어쿼리에 이미 있으므로 중복 선언하지 않도록 하나로 합친다 — 같은 `@media` 블록 안에 두 번 선언되어 있다면 이 Task에서 하나로 병합한다.)

- [ ] **Step 2: 브라우저 개발자도구에서 모바일 너비(예: 375px)로 확인**

개발자도구 반응형 모드로 375px 너비에서 nav, hero, Step2 카드 필드(2열), Step3 항목 줄바꿈이 깨지지 않는지 확인.

- [ ] **Step 3: Commit**

```bash
git add "알릴의무 프로젝트/style.css"
git commit -m "style(알릴의무): 모바일 반응형 마무리"
```

---

## Task 10: 종단 수동 검증 (README 시나리오 재현)

**Files:** 없음 (검증 전용 태스크)

- [ ] **Step 1: README 5장의 버그 재현 케이스로 회귀 테스트**

textarea에 다음을 입력:
```
🔴고혈압/I10
입원: 없음
수술: 없음
치료/현상태: 22.03.10~
```
"표로 변환" 클릭 → Step2 카드의 최근진료일이 비어있고 "확인필요" 배지가 붙어있는지 확인 (오늘 날짜로 자동 채워지면 회귀 버그 — Task3에서 막은 동작이 깨진 것).

- [ ] **Step 2: README 2장의 다중 문항 배치 케이스 확인**

다음을 입력:
```
🔴고혈압/I10
입원: 없음
수술: 없음
치료/현상태: 26.05.01 진단, 현재 약물치료중
```
변환 후 Step2에서 상시복용여부 체크박스를 체크 → Step3에서 같은 "고혈압" 항목이 Q2와 Q5(11대질병, 최근진료일 5년 이내)에 동시에 나타나는지 확인.

- [ ] **Step 3: 전체 단위 테스트 마지막 실행**

Run: `node "알릴의무 프로젝트/test/parser.test.js" && node "알릴의무 프로젝트/test/classify.test.js"`
Expected: 둘 다 전체 통과, exit code 0

- [ ] **Step 4: 최종 커밋(필요 시)**

검증 중 버그를 고쳤다면 해당 수정 파일만 추가 커밋:

```bash
git add -A "알릴의무 프로젝트"
git commit -m "fix(알릴의무): 종단 검증에서 발견된 이슈 수정"
```

수정사항이 없다면 이 커밋은 생략한다.
